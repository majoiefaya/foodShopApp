{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}
{% block class %} sub_page {% endblock %}

{% block ListMenu %}
    <h1>Categorie</h1>

     <table class="table table-striped table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie') }}" class="btn btn-primary">Page Categorie</a>

    <a href="{{ path('ModifierCategorie', {'id': categorie.id}) }}" class="btn btn-primary">Modifier</a>

{% endblock %}
