{% extends 'base.html.twig' %}
{% block class %} sub_page {% endblock %}
{% block ListMenu %}

<section class="book_section layout_padding">
    <div class="col-md-6">
        <div class="container">
            <div class="heading_container">
                <h2>Creation D'un Responsable</h2>
            </div>
        <div class="form_container">
    {{ include('responsable/_form.html.twig') }}
</div>
</div>
</div>
</section>
    <a href="{{ path('responsable') }}" class="btn btn-danger">Retourner a la liste</a>
{% endblock %}
