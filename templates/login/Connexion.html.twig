{% extends 'base.html.twig' %}
{% block class %} sub_page {% endblock %}

{% block ListMenu %}
<section class="book_section layout_padding">
  <div class="col-md-6">
      <div class="container">
          <div class="heading_container">
              <h2>Connexion</h2>
          </div>
      <div class="form_container">

  <form role="form" method="post" action="{{path('login')}}" onsubmit="" class="trip-form">

    {% if error %}
        <div class="alert alert-primary" role="alert">Authentification Echouée</div>
    {% endif %}
    <div class="d-flex justify-content-center">
      
      <div class="mb-3 mb-md-0 col-md-3">
        <div class="form-control-wrap">
          <input type="texte" id="cf-4" placeholder="UserName" name="_username" value="{{LastUsername}}" class="form-control">
        </div>
      </div>
      
      <div class="mb-3 mb-md-0 col-md-3">
        <div class="form-control-wrap">
          <input type="password" id="cf-4" placeholder="Password" name="_password" class="form-control">
        </div>
      </div>
      <button class="btn btn-primary">Connexion</button>
      <input type="hidden" name="_target_path" value="{{path('admin')}}">

    </div>
    
  </form>
</div>
</div>
</div>
</section>

{% endblock %}


