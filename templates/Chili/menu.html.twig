<header class="header_section">
    <div class="container">
      <nav class="navbar navbar-expand-lg custom_nav-container ">
        <a class="navbar-brand" href="{{path('home')}}">
          <span>
            <img src="{{asset('images/Mr Burger.png')}}" alt="" srcset="" width="100px">
          </span>
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class=""> </span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav  mx-auto " style="font-family: 'Dancing Script', cursive; font-weight: bold">
            <li class="nav-item" >
              <a class="nav-link" href="{{path('home')}}">Acceuil <span class="sr-only">{% block active %}{% endblock active %}</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('Menu')}}">Menu</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('APropos')}}">A Propos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('Contact')}}">Contact</a>
            </li>
            {% if app.user %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('admin')}}">Admins</a>
            </li>
            {% endif %}
          </ul>
          <div class="user_option">
          {% if app.user %}
            <a href="{{path('security_logout')}}" class="user_link">
              <i class="fa fa-user" aria-hidden="true"><p style="font-family: 'Dancing Script', cursive; font-weight: bold">Deconnexion</p></i>
            </a>
          {% endif %}
            <a href="" class="order_online">
              Visiter Mr Pizza
            </a>
          </div>
        </div>
      </nav>
    </div>
  </header>